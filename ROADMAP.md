# Enlil — Roadmap

## Phase 0 — Project Scaffold & Dev Environment
- [x] 0.1 Cargo workspace scaffold (all crates, portability trait, CI, toolchain, rust-vmm deps)
  - [x] Cargo workspace with resolver 2, edition 2024, all top-level crates (enlil-platform/hal/core/devices/config/mgmt/boot/setup/std)
  - [x] Define architecture-neutral HypervisorBackend trait in enlil-hal
  - [x] GitHub Actions CI running cargo fmt --check, clippy -D warnings, and test
  - [x] Pin nightly toolchain (rust-toolchain.toml with rustfmt/clippy/rust-src + x86_64 targets)
  - [x] Add rust-vmm crates (kvm-ioctls, vm-memory, vm-superio, linux-loader) to enlil-core
- [x] 0.2 Minimal KVM-backed VMM: create VM/vCPU, load kernel, KVM_RUN loop, handle exits, serial console
  - [x] KvmBackend over kvm-ioctls (new/map_memory/create_vcpu/run_vcpu) with GuestExit model + VmExitHandler trait
  - [x] DeviceBus (PIO/MMIO) dispatch implementing VmExitHandler as the single decode path
  - [x] 16550 UART serial console (COM1 0x3F8) as a bus device, interrupt-capable via IrqLine
  - [x] 8254 PIT bus device (0x40-0x43) with read-back command
  - [x] Legacy PCI config mechanism #1 (0xCF8/0xCFC PciConfigIo) + ECAM over PcieRootComplex
  - [x] Interrupt controllers: 8259 DualPic, I/O APIC, LAPIC, shared interrupt delivery + MADT/ELCR/PIRQ
  - [x] standard_pc_complete assembling full legacy PC (RTC, PS/2, ACPI PM blocks, DMA, chipset ports, HPET)
- [ ] 0.3 USB live boot & non-destructive testing (qcow2 overlays, UEFI/OVMF payload, first-run setup wizard)
  - [x] Writable qcow2 overlay backend: read-only base + COW overlay, backing chain, refcount consistency check
  - [ ] UEFI boot application (/EFI/BOOT/BOOTX64.EFI) that loads config + OVMF from USB/ESP
  - [ ] ESP layout + OVMF firmware/vars plumbing and config for guest boot
  - [ ] enlil-setup first-run wizard: real hardware detection (/sys, /proc) + interactive TUI prompts — real detection progressing: CPU count + RAM from /proc (landed earlier), and now NVMe drives from /sys/class/nvme (with model labels) and IOMMU groups + their member devices from /sys/kernel/iommu_groups, and GPUs + USB controllers + NICs by PCI class (0x03 display / 0x0c03 USB / 0x02 network) from /sys/bus/pci/devices labelled vendor:device [BDF], overlaying the stub with honest (possibly-empty) data; pure classifiers/formatters (nvme_label, format_iommu_group, is_display_controller, is_usb_controller, is_network_controller, pci_device_label) unit-tested. So all five device lists + CPU/RAM are now live sysfs/procfs reads. STILL STUBBED: the interactive TUI prompts (run_wizard returns a fixed example)
  - [x] enlil-setup writes config.toml from wizard output
- [ ] 0.4 Documentation: README (crate structure, layer stack, migration plan) + developer docs tooling
  - [x] README.md documenting crate structure, layer stack, and KVM-to-bare-metal migration/phase plan
  - [ ] Set up mdbook (or similar) for developer docs

## Phase 1 — Enlil Platform Layer & Custom `std` Target
- [x] 1.1 Build the enlil-platform adaptation-layer crate (memory, threading, sync, time, I/O, async modules + init/backend_name)
  - [x] enlil-platform crate with async_rt, io, memory, sync, threading, time modules
  - [x] platform init() dispatching to linux/baremetal backends + backend_name()
- [ ] 1.2 Create x86_64-unknown-enlil custom target and cross-compile std through enlil-platform std::sys bindings
  - [x] x86_64-unknown-enlil.json target spec file
  - [ ] Wire -Z build-std=std,core,alloc,panic_abort (.cargo/config.toml)
  - [ ] Implement std::sys platform bindings in enlil-platform
- [x] 1.3 Memory subsystem — global allocator plus physical/virtual memory management
  - [x] Hybrid GlobalAlloc: buddy allocator + slab cache, registered as PlatformAllocator
  - [x] Physical memory manager: PhysAddr/VirtAddr/PhysFrame types + bitmap frame allocator
  - [ ] Region carving from UEFI/host map (hypervisor heap, guest RAM, DMA, MMIO) — backend-neutral model + algorithm landed (memory::map: MemoryKind/MemoryRegion with end/contains/overlaps, MemoryMap kept sorted with total_usable/largest_usable, and carve/carve_below first-fit that splits a usable region, retypes the carved span Reserved, and honors alignment + a below-limit constraint for a 32-bit DMA window; is_consistent invariant). Region-assignment policy landed too: MemoryMap::plan_hypervisor_regions(MemoryPlanRequest) carves a low DMA window (<4 GiB), the hypervisor heap, and one disjoint RAM region per guest (LOCKED PRINCIPLE 5), all-or-nothing on a scratch copy so a partial plan never mutates the map; unit-tested. E820 parser landed too: MemoryMap::from_e820(&[E820Entry]) maps the raw E820 type codes (1 usable, 2 reserved, 3 ACPI-reclaimable, 4 ACPI-NVS, 5 bad; unknown→Reserved) to MemoryKinds, drops zero-length entries, and sorts. UEFI conversion landed: MemoryMap::from_uefi(&[UefiMemoryDescriptor]) maps EFI_MEMORY_TYPEs (loader/boot-services/conventional 1-4,7 → Usable; 8 → Bad; 9/10 → ACPI reclaim/NVS; else Reserved), converts pages→bytes, drops zero-page descriptors, sorts. NEXT SLICE: wire the boot payload to collect the real UEFI map and feed it (Phase 6.1)
  - [ ] Virtual memory: host page tables (CR3), mmap-like semantics, guard pages — x86-64 page-table builder landed (enlil-platform::memory::paging): flags module (PRESENT/WRITABLE/USER/HUGE_PAGE/NO_CACHE/GLOBAL/NO_EXECUTE…), pml4/pdpt/pd/pt_index address splitters, table_entry/huge_2mib_entry constructors, and build_identity_map_2mib(mem, tables_base_gpa, bytes, leaf_flags) which lays out PML4→PDPT→one-PD-per-GiB of 2 MiB huge-page identity map into a buffer and returns PageTableLayout { cr3, table_count, bytes } — the reusable form of the orchestrator's inline long-mode trampoline tables; validates alignment, ≤512 GiB (single PML4 entry), and buffer fit; unit-tested. Wired into the live path: GuestRuntime::prepare_long_mode now builds its identity trampoline tables via build_identity_map_2mib instead of hand-rolled entries (enlil-core gained an enlil-platform dep) — byte-identical output, verified by the real-KVM orchestrates_a_long_mode_guest test still booting a 64-bit guest. 4 KiB-granular mapping + guard pages landed: build_identity_map_4kib(mem, base, num_pages, leaf_flags, guard_pages) builds a full PML4→PDPT→PD→PT map (≤512 pages / 2 MiB per slice) leaving each guard-page index not-present so a touch faults, plus a pte_entry constructor; unit-tested. Multi-table variants landed too: build_identity_map_4kib_multi lays out one PT per 512 pages + one PD per 512 PTs for a full 4 KiB-granular map up to one PDPT (512 GiB), and build_identity_map_2mib_multi lays out one PD per GiB + one PDPT per 512 GiB for a 2 MiB-granular map up to one PML4 (256 TiB, via PagingError::MapExceedsAddressSpace beyond that) — the primitives for identity-mapping a big-memory host/pooled node; byte-identical to the single-table builders on small maps, unit-tested (600-page two-PT map, 513 GiB two-PDPT map, boundary + rejection cases). NEXT SLICE: a Mapper that composes ranges at mixed granularity (2 MiB + 4 KiB) at non-zero base addresses
- [x] 1.4 Threading — per-CPU run queues, priority scheduling, and work-stealing
  - [x] Priority levels (Critical>High>Normal>Low), Task, per-CPU RunQueue
  - [x] Work-stealing scheduler (Scheduler / BareMetalScheduler) + CPU affinity
  - [x] std::thread::spawn backend (spawn_hosted) + per-CPU TLS (CpuLocal)
  - [ ] Real context switch with VMX/XSAVE state + APIC-timer preemption
- [x] 1.5 Synchronization primitives — Mutex, RwLock, Condvar, and MPSC channels
  - [x] Platform Mutex + RwLock (std/spin backends) with guards
  - [x] Condvar with wait/wait_while/notify
  - [x] Bounded MPSC channel with Send/TrySend/Recv error types
- [x] 1.6 Async runtime — lightweight priority-aware executor with waker and reactor
  - [x] Executor + PriorityExecutor with Wake-based re-enqueue and block_on
  - [ ] I/O reactor (interrupt/epoll-driven waker, IPI wakeups) — backend-neutral core landed (async_rt::Reactor: Mutex-guarded registration table with unique never-reused tokens, set_waker arming that fires immediately on an already-ready source to close the poll/arm race, mark_ready enqueue+wake fired outside the lock, take_ready draining, deregister; unit-tested with a counting waker). Linux epoll source landed: async_rt::epoll::EpollPoller owns an epoll instance (EPOLL_CLOEXEC), add/add_reader/modify/remove register fds against reactor tokens (stored in the epoll event data), and poll(&reactor, timeout) blocks in epoll_wait then calls reactor.mark_ready(token) per ready fd (EINTR → Ok(0)); libc dep gated to target_os=linux so the bare-metal target never pulls it; unit-tested on real Linux with pipes (readable fd marks its token, remove stops events, two fds → two tokens). EpollPoller::poll is now wired into the executor's run loop: Executor::run_with_poller drains every runnable task, then — while any task is parked on a reactor-registered source — blocks in EpollPoller::poll (which marks ready fds and fires the parked tasks' wakers), returning when the reactor empties; this replaces the sourceless Reactor::wait sleep fallback with real kernel-blocking readiness (gated to platform-linux+target_os=linux). Verified on real Linux: a task parks on a nonblocking pipe, the loop blocks in epoll_wait, and a byte written from another thread wakes it to completion. NEXT SLICE: the bare-metal event source — device interrupt + IPI wakeups driving the same mark_ready
  - [ ] Priority inheritance on lock contention — tracker landed (threading::scheduler::PriorityInheritance: tracks a lock holder's base priority + the multiset of blocked-waiter priorities and reports the effective donated priority = highest-priority waiter, where highest is the smallest Priority discriminant Critical=0 so donation is Ord::min; add_waiter/remove_waiter with multiset semantics keep the boost until the last equal waiter leaves, set_base tracks holder changes; unit-tested); NEXT SLICE: wire it into the platform Mutex — arm a waiter on block, drop it on acquire, and re-target the scheduler at the effective priority
- [x] 1.7 Time subsystem — monotonic Instant/Duration, sleep, and TSC calibration
  - [x] Instant/Duration with monotonic ordering + sleep + Stopwatch
  - [x] TSC frequency calibration via CPUID leaf 0x15
- [x] 1.8 I/O trait layer — PlatformIo registry, console, serial/framebuffer backends, logging
  - [x] PlatformIo trait + Console + print!/println! macros
  - [x] SerialPort (16550) and FramebufferConsole backends
  - [x] log crate integration (PlatformLogger) + per-subsystem structured tags
- [x] 1.9 Dual-backend strategy — platform-linux / platform-baremetal compile-time feature gating across all modules
- [ ] 1.10 Port allocator/context-switch/sync/event/time components from Redox OS (relibc, ralloc, kernel context)
- [x] 1.11 Milestone — prove full std API surface (thread/Mutex/Vec/HashMap/async/println/Instant) works via platform layer and compiles for baremetal
  - [x] Milestone binary exercising all std APIs on platform-linux backend (milestone_1_11.rs)
  - [ ] Same binary compiles for platform-baremetal / real x86_64-unknown-enlil std build

## Phase 2 — Multi-Guest CPU & Memory Partitioning
- [x] 2.1 Guest configuration system — TOML guest defs (name, cpus, memory, kernel/initrd/cmdline, scheduling, serial) parsed in enlil-config with CPU/memory-overlap validation
  - [x] EnlilConfig/GuestConfig/HypervisorConfig TOML schema with serde parsing
  - [x] validate_config: no CPU overlap/duplicates, memory overcommit, serial-output and disk-share checks
- [x] 2.2 CPU core dedication — one OS thread per vCPU pinned 1:1 to a physical core with crafted CPUID topology exposing only assigned cores
  - [x] AffinityMask + sched_setaffinity pinning and launch_vcpu_thread (per-vCPU pinned thread) in enlil-core/affinity.rs
  - [x] CPUID interception crafting guest core/package/cache topology (leaf 0x1/0x4/0xB, AMD 0x8000001D/1E) in stealth/cpuid.rs, applied via KVM set_cpuid2
- [ ] 2.3 Time-slicing fallback scheduler — cooperative per-core vCPU time-slicing with full state save on quantum expiry and APIC/TSC-deadline preemption
  - [x] scheduling = dedicated|timeslice|auto config plus resolve_scheduling auto dedicate-or-timeslice logic
  - [x] TimeSliceScheduler run-queue with round-robin switch_next and quantum_ms
  - [ ] Full vCPU context save on quantum expiry (registers, MSRs, FPU/SSE/AVX via XSAVE) — backend primitive landed: KvmBackend::save_vcpu_state/restore_vcpu_state capture+reapply a KvmVcpuState (kvm_regs, kvm_sregs, kvm_xsave, and the context-relevant MSRs — FS/GS/KERNEL_GS base, STAR/LSTAR/CSTAR/FMASK, SYSENTER trio, PAT; IA32_TSC deliberately excluded to not fight the stealth TSC offset; host-unsupported MSRs skipped per-read), verified by a real-KVM save→diverge(protected-mode + LSTAR sentinel)→restore round-trip. Scheduler seam landed: TimeSliceScheduler is now generic over the per-vCPU context type C (default VcpuContext) so it stays host-agnostic — the KVM run loop uses TimeSliceScheduler<KvmVcpuState>, a bare-metal backend its own VMCS/VMCB type, neither leaking into vcpu.rs (LOCKED PRINCIPLE 2/3). switch_saving(outgoing) stores the outgoing vCPU's freshly captured context, advances round-robin, and hands back VcpuSwitch { guest, vcpu, restore } — restore taken out of the store so the run loop owns the live state (None the first time a vCPU runs; the just-saved state for a single-vCPU core); unit-tested with a stand-in context. NEXT SLICE: call it from the live multi-vCPU KVM run loop — backend.save_vcpu_state into switch_saving on quantum expiry, restore_vcpu_state from VcpuSwitch.restore before resuming (also the enabling primitive for 5.7 S3 resume)
  - [ ] APIC-timer / TSC-deadline preemption to force the context switch
- [x] 2.4 Memory isolation — per-guest physical allocator carving host RAM (GPA 0 each) over KVM memory slots, with EPT write-protect + dirty-bitmap primitives
  - [x] MemoryManager physical allocator (2MB-aligned per-guest regions, hypervisor reserve, GPA0->HPA map) in enlil-core/memory.rs
  - [x] KVM memory-slot registration (map_memory / set_user_memory_region) in kvm_backend.rs
  - [x] EptManager first-class ept_set_write_protect / ept_clear_write_protect / ept_get_and_clear_dirty_bitmap + dirty tracking in enlil-core/ept.rs
- [x] 2.5 Per-guest serial console — emulated COM1 per guest multiplexed to separate stdout/pty/file sinks
  - [x] Per-guest 16550 UART emulation (UartState/SerialPort as PIO device) in enlil-core/serial.rs
  - [x] SerialMultiplexer routing each guest's I/O to its own output sink (stdout/pty/file/null)

## Phase 3 — Virtual Device Layer & Storage
- [x] 3.1 VirtIO block device over an abstract StorageBackend trait (qcow2 + raw file)
  - [x] StorageBackend trait (read/write/flush/trim/capacity)
  - [x] QcowBackend + RawFileBackend implementations
  - [x] virtio-blk device wired to backends (enlil-devices/block.rs)
  - [ ] NVMe namespace passthrough via VFIO when IOMMU available
- [x] 3.2 VirtIO net device plus in-hypervisor virtual switch with inter-guest fast path
  - [x] virtio-net backend device
  - [x] MAC-learning virtual switch (unicast forward + broadcast flood)
  - [x] host TAP uplink for external connectivity
  - [ ] SR-IOV NIC passthrough when available
- [x] 3.3 Interrupt virtualization — per-guest IOAPIC/LAPIC and MSI/MSI-X delivery
  - [x] virtual IOAPIC + LAPIC per guest
  - [x] MSI/MSI-X message delivery (deliver_msi)
  - [ ] APICv / posted-interrupt (Intel) and AVIC (AMD) acceleration
- [x] 3.4 Virtual timer & clock — PIT, HPET, TSC offset, paravirt clocks
  - [x] emulated PIT (i8254) and HPET
  - [x] per-guest TSC offsetting (TSC starts at 0, no host leak)
  - [x] KVM clock + Hyper-V reference TSC paravirt clock pages
- [x] 3.5 Management console v1 — ratatui TUI in enlil-mgmt over a Unix socket
  - [x] per-guest CPU/memory usage display
  - [x] serial console access with per-guest tabs
  - [x] start/stop/reboot guest controls
  - [x] Unix-socket protocol to enlil-core
- [x] 3.6 Display compositor "Enlil Zones" over a FramebufferSource trait
  - [x] FramebufferSource trait + IvshmemSource/VirtioGpuSource/CompositorOwnedSource
  - [x] Zone / ZoneLayout / ZoneLayoutEngine (grid tiling, find-zone-at)
  - [x] InputRouter focus + zone-boundary input routing
  - [x] HotkeyConfig (cycle layout / next guest / focus) and composite_frame pass
  - [x] per-monitor multi-monitor layouts (Monitor placement + Monitor::at global-point routing; ZoneLayoutEngine::tile_region / tile_monitors tile each display's region at its global origin)
- [x] 3.7 Inter-guest bridge over a BridgeTransport trait (clipboard/DnD/shared-fs/notify)
  - [x] BridgeTransport trait + LocalVirtioTransport (8-queue dispatch)
  - [x] clipboard bridge (3.7.1)
  - [x] drag-and-drop bridge (3.7.2)
  - [x] shared filesystem / VirtIO-fs (3.7.3)
  - [x] notification forwarding (3.7.4)
  - [x] fast inter-guest virtual network via the virtual switch (3.7.5)
  - [x] URL/protocol-handler scheme routing table + resolution logic (3.7.6)
  - [ ] guest bridge-agent packaging/installers (.deb/.rpm/.msi) (3.7.7)
- [x] 3.8 Production platform-clock cadence — advance_platform_clocks drives advance_clocks from guest ref-cycles in the run loop
- [x] 3.9 LAPIC TSC-deadline timer mode (IA32_TSC_DEADLINE install + fire_due_tsc_deadlines against guest TSC)
- [x] 3.10 qcow2 refcount-TABLE growth in storage/qcow.rs (live refcount-table geometry held in interior-mutable AtomicU64/AtomicU32 on QcowBackend so a &self allocation can grow it; grow_refcount_table appends one self-covering arena at EOF — enlarged table + covering refcount blocks built in memory and written in one pass — copies old entries forward, frees the old table's container clusters, and persists the new offset/clusters into the on-disk header so the grow survives reopen; a growth needing a slot the enlarged table still can't address is refused before any mutation; validated by writing past a 1-cluster table's reach and re-running check_consistency, incl. after reopen)
- [x] 3.11 Cap the 16550 UART RX FIFO (RX_FIFO_CAPACITY bound + overrun flag)
- [x] 3.12 Wire StealthRunLoop::run_real_mode into a top-level guest-boot/orchestrator binary — DONE: the `enlil-run` binary (enlil-core src/bin/enlil-run.rs) parses <config.toml> <bzImage> [cmdline], loads+validates the config, and calls orchestrator::run_first_guest → run_real_mode, booting the first guest end-to-end. Reusable library seam landed (enlil-core::orchestrator::GuestRuntime): prepare_real_mode(GuestBootSpec) builds the standard PC with a per-guest-seeded vTPM (LOCKED PRINCIPLE 1), installs the StealthRunLoop, allocates+maps guest RAM (owned by the runtime so it outlives the KVM slot; drop order enforced), loads a real-mode payload at the entry, creates+prepares the boot vCPU, applies CPUID stealth before the guest runs (LOCKED PRINCIPLE 1 — clears the CPUID.1:ECX[31] hypervisor bit + own-topology + PMU leaf via CpuidStealthTable::from_host), and run() drives run_real_mode; config-fit validation returns Error::Config. Verified on real /dev/kvm by booting real-mode, protected-mode, AND 64-bit long-mode payloads to LoopOutcome::Halted through the seam (protected-mode reaching high HPET MMIO; long-mode via KvmBackend::prepare_long_mode_vcpu + a minimal PML4→PDPT→PD identity map GuestRuntime::prepare_long_mode writes into guest RAM — the mode a real x86-64 kernel runs in). The config→spec half also landed: GuestBootSpec::from_guest_config maps a GuestConfig's name, memory_mb→bytes, and serial sink (honoring serial.enabled) into a spec. bzImage kernel-loader groundwork started: enlil-core::bzimage::parse_bzimage_header decodes a Linux bzImage's setup header (0xAA55 boot signature + "HdrS" magic, protocol version, setup_sects with the legacy-0→4 normalization, and the computed protected-mode-kernel byte offset (setup_sects+1)*512), plus the PROTECTED_MODE_LOAD_ADDR (1 MiB) constant; unit-tested. bzimage::build_boot_params assembles the Linux boot_params zero page (setup header copy, type_of_loader, cmd_line_ptr, E820 map); and GuestRuntime::load_bzimage places the protected-mode kernel at 1 MiB, the boot_params, and the cmdline into guest RAM and returns a KernelBoot placement (verified on real /dev/kvm by loading a fake bzImage and reading the kernel/cmdline/boot_params back). GuestRuntime::boot_kernel enters it per the boot protocol (KvmBackend::prepare_linux_boot_vcpu: protected mode at the 1 MiB entry, RSI → boot_params); and orchestrator::run_first_guest ties it all together — maps a guest's EnlilConfig to a GuestBootSpec (host-detected LBR platform), prepares the runtime, loads the bzImage, boots the kernel, and runs. VERIFIED END-TO-END on real /dev/kvm: run_first_guest booted a config-driven fake bzImage (protected-mode kernel = hlt) to LoopOutcome::Halted, and the enlil-run binary wraps it. REMAINING ENHANCEMENTS (not blockers): multi-guest orchestration, SMP, a real distro boot (needs a real bzImage), and long-mode (64-bit) kernel entry. This seam also unblocks 4.3's routing_state_from_config guest-setup wiring
- [x] 3.13 Skip the per-entry guest-TSC read when no LAPIC deadline is armed (any_lapic_tsc_deadline_armed gate)
- [x] 3.14 RTC/CMOS wall-clock cadence in run_real_mode (advance_rtc_seconds, capped) so the calendar does not freeze
- [x] 3.15 Drive the RTC periodic interrupt from the platform cadence (Rtc146818::advance_periodic accumulates guest-perceived ns and latches a coalesced tick at periodic_rate_hz; wired into StandardPc::advance_clocks, which the run loop drives per entry)

## Phase 4 — USB Peripheral Routing
- [x] 4.1 Assemble the USB subsystem stack (host monitor to routing engine to per-guest virtual xHCI) as a cohesive module
- [x] 4.2 Host USB enumeration and hot-plug monitor with live device inventory
  - [x] UsbMonitor enumerating devices and tracking connect/disconnect (hot-plug) events
  - [x] Identify devices by VID:PID, serial number, and physical port path
  - [x] Live device inventory queryable by port/VID:PID for the console
  - [x] Concrete Linux sysfs host-source enumeration backend wired to the monitor (usb::sysfs: parse_device/scan_devices/sync_monitor diff a /sys/bus/usb/devices scan into report_connect/report_disconnect; poll() self-throttles to the monitor's poll_interval)
  - [ ] Bare-metal xHCI port-scan host-source backend (the sysfs backend covers the Linux dev path; the bare-metal source lands with Phase 6.5's xHCI driver)
- [x] 4.3 Routing policy engine mapping devices to guests with live re-routing and hot-plug attach
  - [x] Device matchers: VID:PID, vendor-only, port path, serial, class, and default (Any)
  - [x] RoutingTable/RoutingState resolving a device to its target guest by priority
  - [x] Live re-routing of a device between guests at runtime (reassign)
  - [x] HotplugDispatcher bridging monitor callbacks to the run loop and applying rules on plug-in
  - [x] Config-driven [usb.routing] TOML block parsed by enlil-config (UsbConfig + parse_usb_match + validation) and converted to a RoutingTable by enlil-core::usb_routing::routing_table_from_config
  - [ ] Wire routing_state_from_config into the run-loop/guest setup so a configured [usb.routing] populates the live RoutingState — guest-setup caller landed (item 3.12 unblocked it): enlil-core::usb_routing::usb_registry_from_config assembles an XhciRegistry from a UsbConfig + the per-guest SharedXhci handles in one call, and routing_targets enumerates a config's target guests (rule targets + default_guest, sorted/deduped); the assembly validates every routing target has a registered controller and rejects a config that routes to a controller-less guest (NoController surfaces at setup, not first hot-plug); unit-tested. NEXT SLICE: call usb_registry_from_config from the live guest-boot path once multi-guest orchestration builds a StandardPc (and thus a SharedXhci) per guest, and drive a UsbMonitor+HotplugDispatcher::service loop against it
- [x] 4.4 Virtual xHCI (USB 3.x) host controller per guest with TRB-level interception
  - [x] VirtualXhciController with cap/operational/runtime/port registers behind one MMIO window (XhciMmio)
  - [x] Mounted as a discrete Renesas uPD720202 PCI function (1912:0015) at 00:04.0 with INTx/PIRQ delivery
  - [x] Doorbell latch and DMA service draining command/transfer rings against guest memory (service_usb_dma)
  - [x] Guest-resident command ring (CRCR), device contexts (DCBAAP), and transfer rings (GuestRingCursor)
  - [x] Endpoint TD stage machines (Setup/Data/Status control + Normal), Transfer Events, STALL/reset recovery
  - [x] Enable/Disable/Address/Configure Slot and Set TR Dequeue command processing
  - [x] UsbDeviceModel seam with in-process LoopbackDevice and emulated HID boot keyboard
  - [x] libusb (rusb) forwarder (LibusbDevice) forwarding TDs to real host devices
  - [ ] Isochronous endpoint forwarding via async libusb transfer path
  - [ ] Fallback: VFIO whole-controller passthrough via IOMMU with FLR-stability warnings
- [ ] 4.5 Management-console USB controls (device list, reassignment UI, hot-plug notifications) with two-mice/two-keyboards live-reassign milestone
  - [x] USB wire protocol surface (UsbDeviceEntry, UsbDeviceList, UsbHotplugNotice, UsbCommand/UsbAction Reassign+Detach)
  - [x] USB tab view-model (enlil-mgmt::usb_tab::UsbTabState): headless state the render layer drives — device inventory with selection that sticks to a device by bus_addr across hot-plug refreshes, wrap-around cursor nav, reassign_action/detach_action UsbAction builders (no-op when redundant), cycle_target rotating the assignment through available guests skipping the current holder, and a bounded hot-plug notice ring; fully unit-tested without a TTY
  - [x] USB tab controller (enlil-mgmt::usb_tab::UsbTabController): the wire↔view-model bridge — handle_server_message folds UsbDeviceList/UsbHotplugNotice into the UsbTabState (returns whether a redraw is needed, ignores non-USB messages), and reassign_selected/detach_selected/cycle_selected turn keystrokes into ClientMessage::UsbCommand with a monotonic command-id sequence (request_devices builds RequestUsbDevices); unit-tested
  - [ ] TUI USB tab rendering physical devices with their current guest assignment (all logic lives in usb_tab::UsbTabState + UsbTabController; needs the ratatui render layer — note: the enlil-mgmt binary is a CLI today with no interactive ratatui app host yet, despite 3.5's tab claims)
  - [ ] Reassignment interface: bind keystrokes to UsbTabController::reassign_selected/detach_selected/cycle_selected and send the returned UsbCommand over the socket (message-building done; needs the event-loop + socket glue)
  - [ ] Hot-plug notifications rendered in the console (UsbTabController::handle_server_message already folds ServerMessage::UsbHotplugNotice into the notice ring; needs the render)
  - [ ] End-to-end milestone: two mice + two keyboards routed to different guests with live TUI reassignment

## Phase 5 — Windows Guest Support & Transparency
- [x] 5.1 Synthesize per-guest ACPI tables (RSDP/XSDT/FADT/MADT/DSDT/SSDT/MCFG/HPET + AML) with realistic OEM strings
  - [x] Hand-rolled AmlBuilder emitting correct PkgLength, _CRS resource templates, _PRT (APIC+PIC link devices), _S5 package
  - [x] Emit FACS, FPDT+FBPT, and WSMT tables pointed to by FADT/XSDT
  - [x] iasl reference-compiler round-trip integration test (0 errors/0 warnings)
- [x] 5.2 Synthesize SMBIOS/DMI tables (manufacturer/product/serial, CPU brand passthrough, memory config, BIOS vendor)
  - [x] fw_cfg device on the bus (item selector 0x510 / data 0x511) delivering ACPI+SMBIOS to a guest
  - [x] etc/table-loader bios-linker-loader command stream (ALLOCATE/ADD_POINTER/ADD_CHECKSUM/WRITE_POINTER)
  - [x] dmidecode --from-dump validation integration test
- [x] 5.3 CPUID stealth table + live-KVM injection (clear hypervisor bit, out-of-range leaves, vendor/topology/PMU leaves)
  - [x] apply_topology_stealth rebuilds CPUID array (leaf 0xB topology, leaf-1 max-IDs, per-vCPU x2APIC IDs)
  - [x] PMU leaf 0xA + AMD topology triad (0x8000_0008 NC, 0x8000_001D cache sharing, 0x8000_001E SMT)
  - [x] Install the CPUID identity leaves from the stealth table (upsert_identity_leaves folds leaf-0 vendor string, leaf-1 FMS, brand 0x8000_0002-4 into apply_topology_stealth so the guest identity matches the topology/PMU/frequency view)
  - [ ] Cross-vendor identity at runtime: presenting a different vendor (e.g. Intel-on-AMD) needs vendor-appropriate MSR emulation before it is safe — the identity install exists but the live from_host table stays same-vendor
  - [x] Guest sees Invariant TSC (CPUID 0x8000_0007 EDX[8]) preserved through stealth — CONFIRMED on real /dev/kvm: though CpuidStealthTable installs no explicit 0x8000_0007 leaf, apply_topology_stealth patches KVM's get_supported_cpuid (which carries the host's Invariant-TSC bit) rather than rebuilding from the table, so the bit survives to the guest. Verified by kvm_backend::tests::cpuid_stealth_preserves_invariant_tsc (real-mode guest reads 0x8000_0007:EDX[8] after apply_topology_stealth and sees it set; skipped only if the host itself lacks Invariant TSC). No transparency gap
- [x] 5.4 Timing stealth — TSC offsetting, constant-time CPUID table, APERF/MPERF + PMC + LBR shadowing
  - [x] Shadow IA32_APERF/MPERF counters advancing at model rate, hiding VMEXIT overhead (VcpuTimingState)
  - [x] RDPMC / PMC MSR shadowing (Intel + AMD blocks) via PmcState/PmcRateModel
  - [x] LBR save/restore + last-entry sanitization model (Intel MSRs + AMD LBRV pair)
  - [x] StealthMsrRouter + per-vCPU StealthBank wired through StealthRunLoop over /dev/kvm
- [x] 5.5 Virtual TPM 2.0 (CRB interface, MMIO 0xFED40000, per-guest PCR banks/NV storage)
  - [x] CRB register interface + TPM2 command dispatch (Startup/SelfTest/PcrExtend/PcrRead/GetRandom/NvRead/NvWrite)
  - [x] Independent per-guest endorsement key generation (VirtualTpm::seeded derives a distinct GetRandom stream + Endorsement Primary Seed per guest via domain-separated SHA-256; deterministic so the EK is stable across reboots, persisted in the state store)
  - [x] Wire the per-guest TPM seed through standard_pc_complete / guest setup so each guest's vTPM gets a distinct seed (DeviceBus::standard_pc_complete_seeded takes an optional per-guest seed and builds the CRB vTPM via SharedTpm::seeded; VirtualTpm::seed_for_guest / SharedTpm::for_guest derive a stable distinct seed from the guest name — a guest-setup caller passes Some(seed_for_guest(&guest.name)))
  - [x] On-disk persistent TPM state store (BitLocker/Windows Hello): save_state/load_state/with_state_path persist NV storage + endorsement seed + RNG to state_path (PCRs stay volatile), tolerant of a missing/malformed file
- [ ] 5.6 Windows boot path via OVMF — provide/load virtual UEFI firmware image per guest and boot Windows in UEFI mode
- [x] 5.7 Windows-specific virtual devices (HDA audio, PS/2 kbd/mouse, PCIe root complex, ACPI power management)
  - [x] Intel HDA controller + codec device model
  - [x] PS/2 keyboard and mouse (i8042) fallback devices
  - [x] PCIe root complex / host bridge
  - [x] ACPI S3/S4 sleep states advertised in the DSDT (\_S3 suspend-to-RAM, \_S4 hibernate, \_S5 soft-off from canonical SLP_TYP_S3/S4/S5; run loop classifies a committed transition as LoopOutcome::Suspend vs Shutdown) — S3-resume groundwork landed: (a) KvmBackend::save_vcpu_state/restore_vcpu_state capture+reapply full vCPU state (see 2.3), (b) acpi::facs::FacsWaking::from_facs reads the S3 waking state OSPM programs into the FACS (32-bit real-mode waking vector @12, 64-bit X vector @24, 64BIT_WAKE OSPM flag @36) and resume_target() decodes it to ResumeTarget::RealMode/Extended/None per ACPI 6.x §5.2.10, and (c) GuestRuntime::snapshot_vcpu/restore_vcpu wire the vCPU-state primitive into the orchestrator (guest RAM stays live in the owned KVM slot, so a snapshot + that RAM is a complete resume point) — verified on real /dev/kvm by snapshotting at entry, running to hlt, restoring, and re-running (payload executes twice), and (d) GuestRuntime::resume_from_s3(facs_gpa) reads the FACS from the guest RAM it owns, decodes the waking vector via FacsWaking, and re-points the boot vCPU at it — verified on real /dev/kvm by resuming into a real-mode payload at the OS-programmed waking vector (a 64-bit X vector is rejected pending long-mode vCPU prep, an unarmed FACS is rejected). The run-loop-driven half landed: GuestRuntime::run_resuming_s3 runs the boot vCPU and, on a LoopOutcome::Suspend(3) return, auto-discovers the FACS from the guest's live ACPI tables (via resume_from_s3_via_rsdp) and re-points the vCPU at the OS waking vector, then runs again — up to max_resumes cycles; non-S3 outcomes (halt/shutdown/reset/S4/budget) end the run. Verified on real /dev/kvm: one call boots a guest that commits S3 via PM1a_CNT, auto-resumes through RSDP→XSDT→FADT→FACS, and runs its waking-vector payload to Halted. NEXT SLICE: a long-mode vCPU-prepare for the 64-bit X waking vector (resume_from_s3 still rejects ResumeTarget::Extended pending safe identity-table placement in reserved memory)
- [ ] 5.8 Anti-detection test suite (in-guest hypervisor-presence checks, IET divergence, LBR analysis, pafish/al-khaser, AML fuzzing)
  - [x] iasl AML validation gate
  - [ ] In-guest automated detection test suite (CPUID/RDTSC/registry/device/NIC-OUI checks) — host-side NIC-OUI primitives landed (MacAddress::is_hypervisor_oui flags QEMU/Xen/Hyper-V/VMware/VirtualBox/Parallels OUIs, plus is_locally_administered/oui; MacAddress::synthesize_for_guest derives a stable, transparent guest MAC — deterministic domain-separated SHA-256 of the name, forced unicast + locally-administered, escaping any locally-administered hypervisor OUI — the safe default when a guest config leaves its MAC unset) AND config-side rejection landed (GuestConfig.mac field + enlil-config::mac self-contained parse_mac/is_hypervisor_oui/mac_rejection_reason; validate_config rejects a malformed, multicast/broadcast, or virtualization-vendor-OUI guest MAC). CPUID host-side detection primitives landed too (stealth::detection): cpuid_hypervisor_present (CPUID.1:ECX[31]), hypervisor_vendor_from_signature (decodes the leaf-0x40000000 signature → HypervisorVendor Kvm/HyperV/Vmware/VirtualBox/Xen/QemuTcg/Bhyve), and cpuid_reveals_hypervisor combining them, plus cpu_vendor_string/is_genuine_cpu_vendor decoding the leaf-0 vendor (EBX,EDX,ECX order) and flagging a blank/unknown vendor — the shared logic the future in-guest agent uses, with a self-verification test asserting enlil's own CpuidStealthTable clears the present bit and exposes no vendor signature at 0x40000000 (LOCKED PRINCIPLE 1). REAL-KVM verified end-to-end too: kvm_backend::tests::guest_cpuid_hypervisor_leaf_exposes_no_vendor_signature boots a real-mode guest that reads CPUID 0x40000000 through the applied apply_topology_stealth path, emits the 12 signature bytes, and the same detection decoder confirms No hypervisor vendor leaked to the guest (not just the table model). RDTSC timing primitives landed too (stealth::detection): median_cycles + timing_reveals_hypervisor analyze a sample of cycle-count deltas measured across a serializing VM-exit-prone instruction (CPUID between two RDTSCs) — a median (robust against SMI/interrupt outliers) above BARE_METAL_CPUID_CYCLE_CEILING means the op is being trapped and emulated across a VM boundary, the classic timing tell enlil's TSC/APERF-MPERF stealth (5.4) must defeat; unit-tested (bare-metal-with-outliers passes, trapped-CPUID sample flagged). Still need the in-guest agent that runs these (plus registry/device checks) from inside a booted guest
  - [x] Custom IET divergence test using IA32_APERF/MPERF (iet_ratio_does_not_diverge_across_many_interleaved_exits: 1000 interleaved advance/exit cycles, sampled ratio pinned to the model ratio within 1e-3)
  - [ ] pafish + al-khaser runs and anti-cheat (BattlEye/EAC/Vanguard) validation
  - [ ] ACPI AML fuzzing (BadAML)
- [x] 5.9 CPUID 0x15/0x16 TSC/processor-frequency leaves pinned to measured guest tsc_khz, threaded through topology stealth
  - [x] upsert_frequency_leaves pins leaf-0x15 TSC rate to KVM_GET_TSC_KHZ and raises max-basic-leaf 0 EAX to >= 0x16

## Phase 6 — Bare-Metal Boot (UEFI Payload)
- [ ] 6.1 Build the UEFI boot application (uefi-rs) that runs as the firmware boot payload, consumes UEFI services, calls ExitBootServices(), and hands off to the Enlil kernel
  - [x] Add uefi crate dependency and an efi_main entry point in enlil-boot — real #[uefi::entry] efi_main (uefi-rs 0.33), gated behind cfg(target_os = "uefi") so the host dev build still compiles; builds for x86_64-unknown-uefi
  - [ ] Collect UEFI memory map, ACPI table pointers, GOP framebuffer, and PCI protocol handles before exit — ACPI RSDP (config table, prefers ACPI 2.0 XSDT), GOP framebuffer (uefi_boot::collect_framebuffer → handoff::Framebuffer), and the final UEFI memory map (map base/len/descriptor size, captured from the map exit_boot_services returns) are collected and packed into handoff::BootHandoff. NEXT SLICE: also collect PCI protocol handles
  - [x] Call ExitBootServices() to take full hardware control — efi_main calls uefi::boot::exit_boot_services(LOADER_DATA), forgets the owned map so its buffer survives for the kernel, and proves post-exit execution by writing the "enlil kernel alive" banner straight to COM1 (enlil_boot::serial, raw 16550 port I/O, since the firmware logger is gone). First live-boot sub-milestone reached AND boot-verified: scripts/qemu-boot-test.sh boots the payload under QEMU+OVMF (q35, KVM-accelerated, +svm) and observes the banner + RSDP/GOP/memory-map log lines on serial post-ExitBootServices
  - [x] Transition control into the hypervisor kernel — efi_main now ends with enlil_boot::kernel::kernel_entry(&BootHandoff) -> ! : the kernel brings up its own COM1 console, walks the handed-off raw UEFI memory descriptor array (stride-honoring, alloc-free — summarize_memory_map + format_u64 are pure and host-unit-tested) and reports total/largest usable RAM on serial before parking; boot-proven under QEMU+OVMF (the harness now also asserts the kernel's memory line, not just the banner). The kernel body grows from here via the 6.2 items (heap install from the handoff, IDT/APIC, VMX/SVM)
  - [x] Install qemu-system-x86 + ovmf in WSL so scripts/qemu-boot-test.sh can run the nightly QEMU+OVMF boot proof — done WITHOUT root via a user-space install (apt-get download of qemu-system-x86/qemu-system-data/seabios/ipxe-qemu/ovmf + their library debs, dpkg -x into ~/qemu-local/root, an LD_LIBRARY_PATH wrapper at ~/qemu-local/bin/qemu-system-x86_64); qemu-boot-test.sh honors ENLIL_QEMU/ENLIL_OVMF_CODE/ENLIL_OVMF_VARS overrides, probes ~/qemu-local, boots split OVMF_CODE_4M/OVMF_VARS_4M via pflash, picks +svm/+vmx from /proc/cpuinfo, and kills QEMU as soon as the banner lands (~3 s instead of the full 60 s timeout). Bare-metal boot steps are now boot-verified nightly
- [ ] 6.2 Implement the bare-metal kernel: direct VMX/SVM programming plus activation of the platform-baremetal backend
  - [ ] Intel VMX path: VMXON, VMCLEAR, VMPTRLD, VMLAUNCH/VMRESUME, VMREAD/VMWRITE — capability/VMCS decode layer landed in enlil-hal::vmx (the sole ISA seam, no_std + builds for x86_64-unknown-enlil): VmxBasic decodes IA32_VMX_BASIC (revision id / region size / memory type / TRUE-controls); VmxControlCaps decodes the pin/proc/exit/entry control-capability MSRs with adjust() applying the allowed-0/allowed-1 rule; VmcsField models the VMREAD/VMWRITE field encoding (width/type/index) with a curated const set (GUEST/HOST RIP/RSP, CR3, RFLAGS, VMCS_LINK_POINTER, EPT_POINTER, GUEST_PHYSICAL_ADDRESS); VmxExitReason + exit_reason consts decode the exit-reason field; IoExitQualification + EptViolationQualification decode the two exit-qualification interpretations; io_exit_to_vmexit / simple_exit_to_vmexit route I/O, HLT and TRIPLE_FAULT onto the arch-neutral VmExit; init_vmx_region stamps a VMXON/VMCS region's revision header. All pure + host-tested against Intel SDM Vol. 3. NEXT SLICE: the actual VMXON/VMCLEAR/VMPTRLD/VMLAUNCH asm ops + a Vmcs holding a 4 KiB region allocated by the platform
  - [ ] AMD SVM path: VMRUN, VMSAVE/VMLOAD, #VMEXIT handling — capability/VMCB decode layer landed in enlil-hal::svm (mirror of the VMX layer; pure, no_std, host-tested against AMD APM Vol. 2, builds for x86_64-unknown-enlil): SvmFeatures decodes CPUID Fn8000_000A (revision/ASID count/feature bits incl. nested paging + NRIP-save), VM_CR SVMDIS/LOCK gates + EFER.SVME + VM_HSAVE_PA consts, the curated VMCB control- and save-area offset tables (intercept vectors incl. the always-required VMRUN intercept and enlil's CPUID intercept, ASID/TLB, exit code/info, NESTED_CTL/nCR3, NEXT_RIP; segments, EFER, CR0/3/4, RFLAGS/RIP/RSP/RAX, G_PAT), init_vmcb_region, SvmExitCode decode (exception vector, CR read/write index, INVALID), IoioExitInfo + ioio_to_vmexit and simple_svm_exit_to_vmexit routing IOIO/HLT/SHUTDOWN onto the arch-neutral VmExit, NptFaultInfo (NPF error code), and msr_exit_is_write. NOTE: this AMD workstation's QEMU harness exposes nested SVM (+svm), so the second live-boot sub-milestone runs the SVM path, not VMX. NEXT SLICE: the VMRUN/VMSAVE/VMLOAD/STGI/CLGI asm ops + a Vmcb type holding a 4 KiB region from the platform allocator
  - [x] Wire the bare-metal GlobalAlloc to a real heap — PlatformAllocator's bare-metal path (previously a null-returning stub) now backs alloc/dealloc with a spinlocked linked_list_allocator Heap installed by init_baremetal_heap; memory::init_global_heap_from_uefi + map::select_bootstrap_heap_region pick the largest usable region from the raw UEFI descriptor array (alloc-free, before any Vec-backed MemoryMap exists) and install it. The audited intrusive-free-list heap is used rather than the in-crate BuddyAllocator (whose Vec free lists would re-enter the global allocator); buddy/slab remain for page-granular physical allocation above the heap. Host- + baremetal-cfg tested. LIVE IN THE BOOT BINARY too: enlil-boot registers its own switching #[global_allocator] (enlil_boot::allocator — firmware pool while boot services are up, the kernel's linked_list_allocator heap after; post-switch frees of firmware-pool pointers are leaked by design), kernel_entry installs the heap in the largest CONVENTIONAL region from the handoff (type 7 only — loader/boot-services regions still hold the image, stack and map buffer; capped at BOOT_HEAP_MAX_BYTES=64 MiB to leave room for the guest-RAM carve) and proves a Vec allocation with the firmware gone — boot-verified nightly (the QEMU harness asserts the heap line). The boot binary carries its own heap instance until Phase 1.2 lets it link enlil-platform's
  - [ ] Implement per-CPU work-stealing scheduler as the bare-metal threading backend
  - [ ] Implement spinlock/sleep-lock sync primitives for the bare-metal backend
  - [ ] Implement calibrated-TSC time backend for bare-metal
  - [ ] Wire serial port / GOP FramebufferConsole as the bare-metal I/O backend
  - [ ] Host page-table management plus EPT/NPT for guests
  - [ ] Interrupt handling: IDT setup, APIC configuration, APIC preemption timer
  - [ ] Per-CPU data structures (one per core, GS-base TLS); implement baremetal_init() (currently a stub — its first step, memory::init_global_heap_from_uefi, is ready; needs the BootHandoff threaded into init())
- [ ] 6.3 Parse host firmware ACPI/UEFI tables to discover physical hardware and build Enlil's own device tree — READER primitives landed (enlil-devices::acpi::discover): find_table(sig) generic walk over RSDP→XSDT/RSDT, plus discover_host_topology bundling into a HostTopology and now build_device_tree assembling the FULL pipeline into a HostDeviceTree { cpus (CpuNode: MADT APIC ID + SRAT NUMA domain), memory (per-node RAM ranges), numa_distances (SLIT matrix), pci_functions (ECAM walk), xhci_controllers (with MMIO bases), iommu } in one call — each CPU's NUMA node resolved by matching its MADT APIC ID against the SRAT affinities (None when unmatched); bounds-checked, unit-tested end-to-end against a synthetic RSDP→XSDT→MADT/SRAT/SLIT/MCFG/DMAR table set. x2APIC SRAT affinity now parsed too: srat::cpu_affinities handles both Processor Local APIC Affinity (type 0, 8-bit ID) and Processor Local x2APIC Affinity (type 2, 32-bit ID at +8), with CpuAffinity.apic_id widened to u32 so >255-APIC CPUs get a NUMA node; unit-tested. NEXT SLICE for the whole item: feed build_device_tree the *real* host tables (RSDP scan + tables) from the bare-metal boot payload (Phase 6.1).
  - [ ] CPU topology from MADT/SRAT — parsers landed: madt::count_enabled_cpus (Local APIC/x2APIC ENABLED count) + srat::numa_domains (distinct enabled proximity domains) + discover::host_cpu_count/host_numa_domains. NUMA affinity now parsed too: srat::memory_affinities returns each enabled Memory Affinity range as MemoryAffinity { proximity_domain, base_address, length, hot_pluggable, non_volatile }, and srat::cpu_affinities returns each enabled Processor Local APIC Affinity as CpuAffinity { apic_id, proximity_domain } — together they say which cores and which RAM share a node (the north-star working-set-stays-on-one-node primitive); discover::host_memory_affinities/host_cpu_affinities tie SRAT discovery to them; the SRAT walk is factored into one for_each_structure helper. madt::enabled_apic_ids lists the enabled processors' APIC/x2APIC IDs in table order (len == count_enabled_cpus), + discover::host_apic_ids — so a discovered CPU's APIC ID matches to its NUMA node via cpu_affinities. NUMA distances landed: slit::locality_distances parses the SLIT into a LocalityMatrix with distance(from,to)/row(from) accessors (10=local per ACPI, larger=farther; bounds-/overflow-checked against a corrupt count), + discover::host_numa_distances — the interconnect-latency input to placement/fabric (working set stays on one node). Unit-tested (incl. high-byte proximity-domain reassembly, 2-node distance matrix, truncation/mismatch rejection). Still needs the real host tables (6.1)
  - [x] PCI devices via MCFG ECAM config-space walk — enlil-devices::pci_discovery walks the ECAM windows: walk_ecam(mem, &McfgAllocation) flat-scans a window's bus range with standard ECAM addressing (base + (bus-start_bus)<<20 + dev<<15 + fn<<12), reading each function's config header (Vendor/Device ID, revision, class/subclass/prog-IF, Header Type); function 0 first, functions 1-7 only when the multifunction header bit is set, Vendor 0xFFFF/0x0000 treated as absent, out-of-image reads skipped (no panic). PciFunction carries the BDF + identity with is_multifunction/header_layout/is_pci_bridge/is_display_controller/is_usb_controller/is_xhci predicates, plus the subsystem vendor/device IDs (config 0x2C/0x2E, read for general-header functions — the card's OEM identity for the device tree and passthrough matching). walk_ecam_allocations chains all windows; discover::host_pci_functions ties MCFG discovery to the walk. Unit-tested with a synthetic ECAM (host bridge + multifunction VGA/audio + xHCI + absent slot). BAR-size decode landed: memory_bar_size_32/memory_bar_size_64/io_bar_size decode a BAR's region size from the standard all-ones write-probe readback (mask info bits, invert, +1; 0 for unimplemented), the pure decode the live bare-metal sizing path calls after it probes; unit-tested across 16 B / 16 MiB / 256 MiB / 4 GiB. RSDP-scan landed: discover::scan_rsdp searches the EBDA (segment @0x40E) then the BIOS ROM area 0xE_0000..=0xF_FFFF on 16-byte boundaries, accepting only a signature + valid ACPI checksum(s) — this supplies the rsdp_gpa the other host_*/build_device_tree calls take as input on real BIOS hardware; unit-tested (ROM-area, EBDA-pointer, absent/bad-checksum)
  - [x] PCI capability-list walk — pci_discovery::capabilities follows the legacy cap chain from config 0x34 (gated on STATUS bit 4), returning each Capability { id, offset } (POWER_MANAGEMENT/MSI/PCI_EXPRESS/MSI_X id consts); extended_capabilities walks the PCIe extended list from 0x100 returning ExtendedCapability { id, version, offset } (SR_IOV/ACS id consts); both cycle-guarded and out-of-image-safe. is_sr_iov_capable(func) checks for the SR-IOV extended cap — the primitive the SR-IOV passthrough tiers (3.2 NIC, 7.3 GPU) gate on. msix_table_size(func) decodes the MSI-X Message Control ((control & 0x7FF)+1 vectors) for interrupt setup. Unit-tested (PM→MSI→MSI-X chain, STATUS-bit gating, SR-IOV detection, MSI-X table size + no-MSI-X None, no-extended-region)
  - [x] IOMMU discovery from DMAR (VT-d) / IVRS (AMD-Vi) — discover::host_iommu_kind returns IntelVtd for a DMAR table, AmdVi for IVRS, None otherwise; unit-tested
  - [x] Memory map from UEFI (E820-style conversion) — enlil-platform::memory::map::MemoryMap::from_uefi(&[UefiMemoryDescriptor]) maps EFI types→MemoryKinds, pages→bytes, sorts (companion to from_e820); unit-tested
  - [x] USB controller discovery (PCI enum to xHCI BARs) — pci_discovery::read_bars decodes a function's BARs from config space (32/64-bit memory, I/O, unimplemented; 64-bit BARs consume two slots, reported once; sizes deferred to the live write-probe path). xhci_mmio_base returns an xHCI function's BAR0 memory base; find_xhci_controllers(mem, allocs) enumerates every xHCI function paired with its MMIO base (XhciController); discover::host_xhci_controllers ties MCFG discovery to it. Unit-tested with a synthetic ECAM carrying a 64-bit xHCI BAR + a 32-bit-mem/IO BAR pair
- [ ] 6.4 Program the physical IOMMU directly (VT-d DMAR / AMD-Vi IVRS): DMA remapping tables, per-guest domain device assignment, interrupt remapping
- [ ] 6.5 Write bare-metal host device drivers to replace Linux-managed hardware: xHCI USB, NVMe/AHCI storage, minimal management NIC, GOP framebuffer console
  - [ ] xHCI USB host driver (replaces libusb/VFIO)
  - [ ] NVMe (or AHCI) storage driver
  - [ ] Minimal management-network driver
  - [ ] UEFI-GOP framebuffer driver for the management console
- [ ] 6.6 Boot a privileged lightweight service VM (Xen Dom0-style) that owns hardware drivers, runs the management console, and provides device backends to guests
- [ ] 6.7 Milestone: boot UEFI to Enlil kernel to service VM to guest VMs, with all Phase 2-5 functionality working without a pre-existing OS

## Phase 7 — GPU Sharing
- [ ] 7.1 GPU strategy tier system — detect GPU hardware capabilities and recommend/select a sharing tier (passthrough / SR-IOV / VirtIO-GPU / mediated / time-sliced)
- [ ] 7.2 Tier 1 full GPU passthrough — assign an entire GPU IOMMU group to one guest via VFIO
  - [ ] Enumerate all GPUs and their IOMMU groups via PCI
  - [ ] Assign the full GPU (all functions in the IOMMU group) to a guest
  - [ ] Handle GPU Function Level Reset (FLR) on guest reboot
  - [ ] Load GPU option ROM / vBIOS for the guest
  - [ ] Detect AMD GPU generation and warn about iGPU-passthrough instability and reset bugs
- [ ] 7.3 Tier 2a Intel iGPU SR-IOV — detect SR-IOV-capable Intel iGPU, enable VFs, and assign one VF per guest
  - [ ] Detect Intel iGPU SR-IOV capability via PCI config space
  - [ ] Enable SR-IOV by writing NumVFs to the PCI SR-IOV capability structure
  - [ ] Assign one Virtual Function per guest via IOMMU
- [ ] 7.3b Tier 2b NVIDIA MIG — detect MIG-capable GPUs and create/assign MIG instances via NVML
- [ ] 7.3c Tier 2c AMD GPU SR-IOV via GIM — support AMD GIM VF model when Radeon/Instinct SR-IOV is available
- [ ] 7.3d Tier 2d generic SR-IOV GPU detection — probe PCI SR-IOV capability on all GPUs at boot and enable/assign VFs for any that advertise it (detection primitive landed: pci_discovery::is_sr_iov_capable / extended_capabilities. The cap read + enable-decision layer landed too: sr_iov_capability decodes the SR-IOV register block (Control/InitialVFs/TotalVFs/NumVFs/First VF Offset/VF Stride/VF Device ID/page sizes) and SrIovCapability::vf_routing_id computes a VF's routing ID via RID(PF)+FirstVFOffset+idx*VFStride; plan_enable_vfs validates a requested VF count against TotalVFs and returns the config writes to perform (set NumVFs, then VF Enable|VF MSE) plus the VF routing IDs; select_sr_iov_system_page_size picks the System Page Size bit to program from the Supported Page Sizes mask — all pure/unit-tested. NEXT: perform the writes on the live bare-metal config-space path — set System Page Size, write NumVFs, set the Control bits, wait the settle time, then assign each VF to a guest via IOMMU)
- [ ] 7.3e Tier 2.5 VirtIO-GPU device backend — implement guest-facing VirtIO-GPU device in enlil-devices with virglrenderer integration for Venus/VirGL/vDRM
  - [ ] Venus context: deserialize guest Vulkan commands and execute on host Vulkan driver
  - [ ] VirGL context: translate guest TGSI IR + OpenGL commands via virglrenderer and execute
  - [ ] vDRM native-context passthrough for same-vendor host/guest GPU driver
  - [ ] Zero-copy zone compositor integration rendering guests into compositor-owned textures
- [ ] 7.4 Tier 3 mediated passthrough — intercept GPU command submission and multiplex a shared GPU across guests
  - [ ] Trap GPU BAR/doorbell MMIO writes via EPT/NPT at the command-submission layer
  - [ ] Parse and validate guest command buffers from guest memory
  - [ ] Fair-share scheduler that queues and forwards command buffers to the physical GPU
  - [ ] Vendor-specific command decoders (Intel first, then AMD, then NVIDIA)
  - [ ] VRAM manager — partition VRAM, per-guest page tables, swap on context switch
  - [ ] Display multiplexing with per-guest virtual framebuffers and EDID synthesis
- [ ] 7.5 Tier 4 time-sliced passthrough — round-robin exclusive GPU access with per-guest state save/restore
  - [ ] GPU state save/restore (registers, VRAM via page-table remap, command queue/fence/interrupt state)
  - [ ] Configurable per-guest time quantum with foreground/priority weighting
  - [ ] GPU reset (FLR or engine-level) between guest switches
  - [ ] Drain/sync command queues at switch points before switching
  - [ ] Start with paused-rendering model (only foreground guest renders), evolve to true VRAM-remap time-slicing
- [ ] 7.6 GPU configuration schema — [gpu] TOML block for strategy/passthrough/timeslice/mediated (device BDF, quantum, priorities, VRAM split)

## Phase 8 — Polish, Hardening & Advanced Features
- [ ] 8.1 Runtime live migration between GPU-sharing strategies (e.g. timeslice→passthrough on guest shutdown) plus live USB device reassignment
- [ ] 8.2 Audio subsystem — per-guest VirtIO-sound device plus hypervisor mixing engine
  - [ ] Present a VirtIO-sound device to each guest (spec 1.2+) with PCM stream queues
  - [ ] Hypervisor audio engine: receive per-guest PCM, mix with sample-rate conversion, output to physical hardware at <10ms latency
  - [ ] Per-guest volume/mute control via console and hotkeys; single-guest microphone routing with hotkey switch
  - [ ] Full audio-device IOMMU passthrough mode (single guest)
  - [ ] Enlil Zones per-zone audio focus (focused zone gets mic priority + louder audio)
- [ ] 8.3 Physical display routing — assign each monitor to a guest, EDID/resolution hot-plug handling, KVM-switch hotkey to swap guest-to-monitor mapping
- [ ] 8.4 Suspend/resume and hypervisor live-update
  - [ ] Save full guest state to disk (hibernation) and resume after host power cycle
  - [ ] Hypervisor live-update: capture all VM state, hot-swap the Enlil binary in memory, restore VMs without guest reboot
- [ ] 8.5 Performance monitoring — collect per-guest CPU, memory-pressure, GPU, and IO throughput metrics and expose them via the management console
- [ ] 8.6 Security hardening — runtime verification that IOMMU is active/enforced (block inter-guest DMA) plus fuzzing of guest/virtual-device interactions
- [ ] 8.7 Confidential-VM support (AMD SEV-SNP and Intel TDX) with vendor-independent attestation
  - [ ] SEV-SNP: C-bit NPT encryption marking, GHCB protocol, #VC handling, ASP attestation reports, VMPL paravisor support
  - [ ] TDX: SEAMCALL/TDCALL handling, TDX-Module-controlled EPT, TD Partitioning (1.5), Intel attestation quoting
  - [ ] ZK-proof attestation of hypervisor binary hash + isolation config via RISC Zero / SP1 zkVM
  - [ ] Threshold/quorum attestation over a cluster (threshold-signature scheme) anchoring genuine-Enlil identity
- [ ] 8.8 Paravisor mode — run device backends inside the guest at higher privilege (VMPL0 / TD partitioning) to support unenlightened guests
- [ ] 8.9 Cross-guest isolation verification — generate ZK proofs of non-overlapping EPT/NPT mappings, disjoint IOMMU domains, and exclusive USB/GPU assignment, with a lightweight guest-side verifier
- [ ] 8.10 Laptop & mobile hardware support
  - [ ] Single-GPU paths: Intel iGPU SR-IOV, AMD VirtIO-GPU Venus, and iGPU+dGPU (Optimus/Switchable) passthrough with mux handling
  - [ ] WiFi management via service VM (NetworkManager/wpa_supplicant) bridged to guests over VirtIO-net virtual switch
  - [ ] Virtual ACPI battery device (_BST/_BIF) mirroring physical battery; low-battery power policy (pause/throttle/notify)
  - [ ] Lid-close sleep/suspend (S3) and hibernation (S4) with passthrough-device resume sequences
  - [ ] Thunderbolt/USB4 device-tree enumeration, per-device routing, eGPU GPU hot-plug, security-level confirmation
  - [ ] Webcam routing (single guest) and optional split virtual V4L2/DirectShow camera
  - [ ] Built-in keyboard/trackpad (PS2/I2C-HID) input routing via Enlil Zones plus hypervisor-level Fn/media/brightness key handling
- [ ] 8.11 Android / mobile guest support
  - [ ] Approach A: boot Android-x86/BlissOS as a full Enlil guest with VirtIO-GPU Venus and emulated touchscreen input
  - [ ] Approach B: enable Waydroid inside a Linux guest (ensure binder/namespaces + VirtIO-GPU acceleration)
  - [ ] Approach C: lightweight ARCVM-style Android with per-app compositor integration (long-term)
  - [ ] Scrcpy/ADB phone-mirroring into a zone
  - [ ] Enlil Bridge Android integrations: clipboard, notifications, file sharing (VirtIO-fs), URL routing
- [ ] 8.12 Snapshot & rollback system built on a shared checkpoint engine
  - [ ] Pluggable CheckpointDestination trait plus FileDestination (local disk) implementation
  - [ ] Full VM-state capture (vCPU regs, MSRs, VMCS/VMCB, EPT/NPT, guest RAM, device model state) serialized/zstd-compressed with copy-on-write memory snapshotting
  - [ ] Disk snapshots for qcow2 (internal) with documented raw/NVMe-passthrough limitations
  - [ ] CLI + TUI operations: snapshot create/list/restore/delete; auto-snapshot before OS updates
- [ ] 8.13 Tiered plugin/extension system (native + WASM + script) and management API
  - [ ] Native Rust-crate plugin tier: VirtioDevicePlugin, GpuStrategyPlugin, VcpuExitPlugin extension traits compiled in via Cargo features
  - [ ] WASM plugin tier: embedded Wasmtime AOT runtime, WASI sandbox, Enlil host-API imports, hot-reload, per-call timeout enforcement
  - [ ] Script plugin tier: embedded Rhai (or Lua/mlua) for event hooks and automation with timeouts
  - [ ] Management API over Unix socket / TCP (REST-JSON and gRPC-Protobuf) covering guest lifecycle, resources, USB, display, metrics, plugin mgmt, and event stream, with Prometheus export

## Phase 9 — Compute Fabric (Heterogeneous Work Routing)
- [ ] 9.1 Define the compute fabric architecture — guest ICD → VirtIO channel → fabric (kernel registry, work router, compilation cache) → CPU/GPU backends
- [ ] 9.2 Build the guest-side Enlil Compute ICD that captures SPIR-V and dispatches, forwarding them over VirtIO
  - [ ] Vulkan ICD: capture SPIR-V at vkCreateComputePipelines, package buffers/dims/push-constants at vkCmdDispatch, block on completion via shared memory
  - [ ] OpenCL ICD: capture kernel/SPIR-V at clCreateProgramWithSource/IL and dispatch at clEnqueueNDRangeKernel
  - [ ] CUDA shim (stretch): intercept cudaLaunchKernel and translate PTX → SPIR-V
  - [ ] Ship + register ICDs on Linux (/etc/vulkan/icd.d, /etc/OpenCL/vendors) and Windows (registry keys)
- [ ] 9.3 Implement the new VirtIO compute device with Submit/Complete/Control queues and the submit/completion descriptor formats
- [ ] 9.4 Build the hypervisor-side compute fabric core: kernel registry, SPIR-V metadata analysis, and dual-backend compilation cache
  - [ ] Kernel registry: index kernels by SHA-256 of SPIR-V, analyze access patterns / arithmetic intensity / control flow / shared-mem / capabilities
  - [ ] GPU-variant compilation: SPIR-V → native GPU ISA via host Vulkan pipeline (KVM) / vendor compiler (bare metal)
  - [ ] CPU-variant compilation: SPIR-V → x86 SIMD via LLVM SPIR-V backend (inkwell), workgroup→thread and invocation→SIMD-lane mapping, cranelift fallback
  - [ ] Compilation cache: eager/lazy JIT warm-up and on-disk caching keyed by SPIR-V hash
- [ ] 9.5 Implement the work router with a ComputeTarget trait, per-dispatch scoring heuristics, and adaptive empirical learning
  - [ ] Define ComputeTarget trait plus LocalGpuTarget and LocalCpuTarget implementations
  - [ ] Scoring heuristics from kernel metadata, queue depth, CPU load, dispatch size, per-guest priority
  - [ ] Adaptive learning: track per-kernel per-backend execution times and switch to empirical routing after N runs
- [ ] 9.6 Implement the CPU backend: LLVM JIT execution with a thread pool mapping workgroups to threads and barriers, over guest memory via EPT/NPT translation
- [ ] 9.7 Implement the GPU backend: dispatch kernels via host Vulkan compute pipeline (KVM) / physical GPU queue (bare metal) with IOMMU-mapped guest buffers
- [ ] 9.8 Implement buffer memory management: zero-copy CPU access, IOMMU-pinned GPU mappings, coherence handling, and confidential-VM bounce/shared-page paths
- [ ] 9.9 Implement cross-guest work stealing (stretch): route one guest's overflow to another guest's idle cores with isolated hypervisor-mediated buffers
- [ ] 9.10 Add the [fabric] configuration schema (routing, cpu_backend, gpu_backend, cache sections)
- [ ] 9.11 Validate end-to-end enablement scenarios: inference overflow, multi-guest load balancing, GPU-failure fallback, GPU-passthrough dev compute
- [ ] 9.12 Implement verifiable compute (stretch): ZK proofs of CPU-backend execution via RISC Zero/SP1, in-guest verifier, sample/always modes, and MPC secret-shared private compute

## Phase 10 — Architecture Portability (ARM & RISC-V)
- [ ] 10.1 Split codebase into arch-neutral vs arch-specific behind the HAL trait — keep config/mgmt/devices/fabric neutral, gate hal/platform impls per arch
- [ ] 10.2 ARM (AArch64) EL2 backend + bare-metal platform port
  - [ ] Implement ArmBackend for the HypervisorBackend trait (EL2, stage-2 page tables)
  - [ ] ARM page-table memory management (4KB/16KB/64KB granules, 4-level tables)
  - [ ] ARM Generic Timer monotonic clock (CNTPCT_EL0)
  - [ ] GICv3 interrupt handling (distributor + redistributor + CPU interface)
  - [ ] ARM SMMU IOMMU for device passthrough
  - [ ] AArch64 UEFI/device-tree boot payload in enlil-boot
- [ ] 10.3 RISC-V (H extension) HS-mode backend + bare-metal platform port
  - [ ] Implement RiscVBackend for the HypervisorBackend trait (HS-mode, hgatp G-stage translation)
  - [ ] RISC-V Sv48 page tables (4-level, 4KB pages)
  - [ ] RISC-V timer via mtime/mtimecmp
  - [ ] PLIC / ACLINT interrupt handling
  - [ ] RISC-V IOMMU support
  - [ ] OpenSBI-based boot payload in enlil-boot
- [ ] 10.4 Bring compute fabric up cross-architecture — validate CPU JIT/VirtIO ICD on ARM/RISC-V and re-tune work-router heuristics

## Phase 11 — Enlil Mesh (Multi-Machine Distributed Hypervisor)
- [ ] 11.1 Network tier classification — latency/bandwidth probe (ICMP+TCP+RDMA verbs+CXL enumeration) that auto-detects and assigns each link to Tier 1 (CXL) / Tier 2 (RDMA) / Tier 3 (LAN) / Tier 4 (WAN), with manual config override
- [ ] 11.2 Peer-to-peer mesh discovery & coordination — no central server
  - [ ] SWIM/Memberlist-style gossip protocol for node discovery, health/failure detection, resource advertisement, and config sync
  - [ ] mDNS LAN auto-discovery + static IP/hostname peers for WAN
  - [ ] Eventually-consistent local mesh view via gossip; Raft only for coordinated live migration
  - [ ] WireGuard encrypted tunnels for inter-node WAN transport (optional plaintext on LAN)
  - [ ] [mesh] config parsing (node_name, listen_port, discovery, static_peers, wireguard_key, trust_mode)
- [ ] 11.3 ZK-verified mesh trust — node join requires ZK attestation before acceptance
  - [ ] Join-time attestation exchange proving genuine binary hash + correct EPT/NPT structure + active IOMMU (~200-byte SNARK verified in ~2ms)
  - [ ] Periodic re-attestation with quarantine of nodes that fail to re-attest
  - [ ] Cross-machine isolation certificates — recursively aggregate per-machine EPT isolation proofs into one cluster-wide proof for spanning guests
  - [ ] WAN partition fail-safe — pause (not crash) guests whose pages span disconnected nodes
- [ ] 11.4 Tier 4 WAN capabilities (20–300ms RTT)
  - [ ] Async compute-fabric offload over WireGuard (serialize SPIR-V+buffers, remote execute, ZK-verify result, return via VirtIO completion queue) with dispatch_time>50×RTT threshold, zstd compression, content-addressed weight caching
  - [ ] Cold migration (pause→transfer state+disk→resume) with qcow2 backing-file dedup
  - [ ] Warm migration (transfer memory→resume→NBD-forwarded disk pulled on demand)
  - [ ] Follow-the-sun time-of-day scheduled migration policy
  - [ ] Eventually-consistent shared filesystem sync (rsync-like delta + conflict resolution)
  - [ ] Remote display streaming via HW H.264/H.265 encode → RTP/RTSP → decode, as an Enlil Zones source
  - [ ] Cross-machine Bridge subset over WAN (clipboard, notification forwarding, URL/protocol routing)
- [ ] 11.5 Tier 3 LAN capabilities (0.1–1ms RTT)
  - [ ] Pre-copy live VM migration with <500ms stun, MAC/IP preservation, virtual-switch MAC update, GPU (VirtIO/passthrough-FLR/SR-IOV) and storage handling
  - [ ] Synchronous interactive compute-fabric offload with dispatch_time>10×RTT threshold
  - [ ] Cross-machine Enlil Zones with uncompressed/LZ4 framebuffer streaming and cross-mesh input routing
  - [ ] Full Enlil Bridge across LAN (clipboard, drag-drop, POSIX-consistent shared-fs, notifications, URL routing, cross-machine virtual switch)
- [ ] 11.6 Tier 2 RDMA fabric capabilities (1–5μs RTT)
  - [ ] Distributed shared memory: EPT page-fault interception fetching remote pages via one-sided RDMA read, Ivy/MESI coherency, page-placement heuristics, NUMA topology exposure, selective spanning config
  - [ ] GPUDirect RDMA zero-copy GPU↔remote-NIC transfers for compute buffers and GPU-to-GPU
  - [ ] Sub-millisecond post-copy live migration over RDMA (pages pulled on-demand)
- [ ] 11.7 Tier 1 CXL fabric capabilities (200–500ns RTT)
  - [ ] Map remote DRAM into guest EPT as CXL range using hardware cache coherency (no DSM protocol)
  - [ ] Transparent multi-machine guests via ACPI SRAT exposing each node's RAM as a NUMA domain
  - [ ] Cross-machine GPU VRAM memory pooling over CXL for virtual multi-GPU
- [ ] 11.8 Mesh compute fabric — multi-machine work routing with mesh-wide GPU inventory (gossip-advertised), latency×performance routing decisions, split dispatches across GPUs with reduction, and ZK verification of untrusted remote results
- [ ] 11.9 Work shares — split a single GPU dispatch across mesh GPUs
  - [ ] Workgroup-boundary dispatch splitting preserving WorkgroupID/buffer offsets
  - [ ] Five splitting strategies: embarrassingly-parallel, data-parallel shard (AllReduce), pipeline stages, spatial decomposition with halo exchange, task-graph DAG scheduling
  - [ ] SPIR-V static-analysis classifier (access patterns, atomics, control flow) selecting strategy and rejecting unsplittable kernels
  - [ ] Split-efficiency/CCR estimation and latency-aware net_speedup scheduling with per-tier profitability thresholds
  - [ ] Distributed buffer management (full replication, range slicing, halo exchange, RDMA on-demand paging, CXL zero-copy) + Blake3 content-addressed buffer caching
  - [ ] Reduction/gather/scatter collectives (tree-reduce, multi-source gather, NCCL-style ring/tree AllGather/AllReduce)
  - [ ] [mesh.compute.work_shares] config parsing (auto_split, min_split_efficiency, max_split_nodes, buffer_cache, strategies, WAN caps)
- [ ] 11.10 Mesh seats — display/input/audio/presence management
  - [ ] Seat model with active-seat tracking (displays, keyboard/mouse, audio bound per node)
  - [ ] Four display modes: Active-Seat-Only, Mirror, Extended-Canvas (LAN), Independent-Stations with per-seat guest assignment and floating/visiting guests
  - [ ] Guest relocation engine: instant redirect (~100ms), live relocate (pre-copy), fast suspend&resume (zstd-compressed), with GPU reassignment handling
  - [ ] Relocation UX across five interfaces (Zones drag-drop, TUI, CLI, REST/gRPC, auto policies) including follow-seat and scheduled policies
  - [ ] Seat switching mechanisms (hotkey, input-activity presence, Bluetooth/WiFi proximity, explicit API) with stream pre-warming
  - [ ] Per-guest seat affinity and seat-locked display
  - [ ] Cross-mesh input routing respecting Zones focus rules
  - [ ] Audio-follows-seat forwarding (VirtIO-sound PCM on LAN, Opus on WAN) and mic routing
  - [ ] Mesh display streaming: LAN raw/LZ4 path and WAN H.264/H.265 path with adaptive quality
  - [ ] [mesh.seats]/[mesh.bridge.clipboard] config parsing (seats, display_mode, switching, clipboard scope/groups)
- [ ] 11.11 Mesh fault tolerance — continuous state mirroring
  - [ ] Level 1 periodic checkpoint replication via EPT COW dirty tracking, zstd delta streaming to backup node
  - [ ] Level 2 continuous (Remus-style) replication at 25–100ms intervals with network output buffering and adaptive checkpoint frequency
  - [ ] Full replica capture (memory deltas, vCPU regs, VirtIO device state, disk, GPU) with disk options (shared / DRBD-style sync / qcow2 delta) and GPU-state handling
  - [ ] Heartbeat-timeout failover with gratuitous-ARP MAC takeover and replica reactivation (~3–5s)
  - [ ] ZK-verified sampling checkpoints (every Nth) with backup-side proof verification and rejection
  - [ ] Multi-node redundancy (1 backup, 2 backups, N/2+1 quorum)
  - [ ] FT + relocation coordination (re-establish backup on different node after move)
  - [ ] Per-guest FT config parsing (ft_level, interval, backup_node, disk_replication, replicas, network_buffering, zk_verify_interval)
- [ ] 11.12 Enlil storage pool — distributed mixed-disk block storage
  - [ ] Boot-time disk benchmark + SMART probe classifying disks into 9 tiers (local/remote NVMe/SATA/HDD/USB)
  - [ ] Block-layer pool with three placement policies (performance-tiered promote/demote, capacity-optimized fill-up, locality-pinned)
  - [ ] Redundancy modes (none, mirror, Unraid-style parity, dual parity, Reed-Solomon erasure coding, cross-machine mirror)
  - [ ] Intelligent disk selector weighing performance, redundancy, SMART health, space pressure, network tier, disk-type
  - [ ] Hot-add/hot-remove/evacuate with mixed size/type/machine support and optional background rebalance
  - [ ] VirtIO-blk pool backend: B-tree LBA→(node,disk,offset) mapper, replicated metadata, network block protocol (WRITE/READ/TRIM), thin provisioning, background tiering daemon
  - [ ] [mesh.storage_pool] config parsing (redundancy, placement, tiering thresholds, per-disk roles, per-guest overrides)
- [ ] 11.13 Mesh security & isolation — optional guest mesh-awareness via VirtIO control queue, CVM (SEV-SNP/TDX) encrypted-page decrypt/re-encrypt for cross-machine DSM, and per-guest mesh_policy enforcement (local-only / lan-ok / any, zk_verify_remote)
- [ ] 11.14 Structure implementation into sub-phases 11a–11f (foundation, migration/relocation, display/bridge, storage pool, fault tolerance, advanced RDMA/CXL fabric) reusing existing subsystems per the dependency map
- [ ] 11.15 Hit milestones M20–M34 (two-node discovery+attestation, LAN/WAN compute offload, cold/live migration, mesh Zones/Bridge, RDMA DSM, CXL coherent memory, Level 1/2 FT, drag-relocation, Independent Stations, cross-machine storage pool, auto tiering)
